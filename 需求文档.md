# 此刻 —— 拖延症快速启动引擎

## 产品定位

一个针对重度拖延症用户的直接行动工具。核心原则：**不提供建议、不询问状态、不给选择**。打开即命令，倒计时逼迫立即开始一个极小的启动动作。

用户画像：重度拖延症患者，面对任务时无法启动，容易陷入思维反刍和回避循环。拒绝任何需要先"分析自己"或"制定计划"才能使用的工具。

**本次明确的功能点**：
1. 注册/登录功能（用户系统）
2. 自定义任务可保存，纳入后续任务池参与派发
3. 不做任务阶梯，只做最直接的任务派发
4. 防回避任务系统设计延后讨论
5. 技术栈：前后端分离，后端 Go，前端 Vue3
6. 任务预设围绕拖延症场景：如何让人快速启动展开
7. 界面语言按浏览器语言自动适配

---

## 核心原则

1. **零前奏摩擦**：从打开到任务开始不超过3秒。登录后无欢迎页、无"你现在感觉如何"。
2. **不给建议，只下命令**：界面不出现"建议你…""你可以试试…"，只出现祈使句，如"打开文档，只看第一行"。
3. **无选择 paralysis**：用户不浏览列表、不选分类、不评分。系统直接派发单一任务。
4. **启动大于完成**：任务的核心是"让身体先动起来"，而非"做完这件事"。
5. **命令式文案**：任务使用祈使句，句尾不加句号，保持向前的动势。多语言由 i18n 支持。

---

## 核心用户流程

```
打开应用
    → 未登录 → 登录/注册页（极简：邮箱+密码）
    → 已登录 → 全屏任务卡片立即出现（系统已自动选好任务）
    → 3秒倒计时覆盖层（3 → 2 → 1）
    → 自动进入执行界面（HUD）
    → 倒计时运行（30秒 / 1分钟 / 2分钟 / 3分钟 / 5分钟）
    → 用户点击"完成了"或"跳过"
        ├─ "完成了" → 简短胜利画面 → "继续"或"结束"
        └─ "跳过" → 直接派发新任务（无批评、无记录）
```

---

## 界面与交互

### 1. 派发页（Landing / Dispatch）
- 居中任务卡片，占视口80%。
- 显示单一祈使句任务 + 时长标签。
- 3秒全屏倒计时覆盖，大字号，不可跳过，点击无效。

### 2. 任务执行界面（Mission HUD）
- 深色极简全屏，无菜单栏、无Logo、无链接。
- 顶部固定显示任务文案，大字号。
- 中央圆形SVG进度环倒计时。
- **"完成了"**：底部正中，大尺寸，绿色，按下有轻微缩放反馈。
- **"跳过"**：底部左下，小尺寸，灰色，低对比度。

### 3. 胜利页（Victory）
- 最多显示3秒，不鼓励停留。
- 大勾 + "完成了"。
- 两个按钮：
  - **"继续"**：立即派发新任务（带3秒倒计时）。
  - **"结束"**：返回空闲状态（显示一个极简的"此刻" logo，点击后重新派发）。

---

## 任务引擎逻辑

### 任务来源
- **系统预设库**：围绕拖延症"快速启动"设计的任务，覆盖工作启动、家务启动、沟通启动、健康启动、行政启动五大类。
- **用户自定义**：允许添加自定义任务，保存后纳入任务池，后续可被派发算法选中。

### 派发规则
- 获取用户可见的所有任务：系统预设任务 + 该用户自定义且 active 的任务。
- 排除最近 5 条执行历史中已出现过的任务。
- 随机选择 1 条。
- **不做复杂加权**：随机 + 近期排除即可。

### 不做任务阶梯
每个任务都是独立、完整、可直接执行的动作。不再将同一目标拆成多级难度。

---

## 任务预设：拖延症快速启动库

**核心理念**：每个任务都是一个"启动动作"，而非"完成目标"。重点在于让身体先动起来，打破"想太多→焦虑→回避"的循环。

### 工作启动类（work）
| 任务文案（中文示例） | 时长 |
|---|---|
| 打开电脑桌面上从左数第一个文件 | 1min |
| 新建一个空白文档，在上面打一个句号 | 30sec |
| 关掉电脑上所有弹出的通知窗口 | 1min |
| 在纸上写下今天的日期 | 30sec |
| 打开邮件客户端，把收件箱第一封邮件标为已读 | 30sec |
| 把桌面上的三个文件选中，拖进一个新建文件夹 | 1min |
| 把电脑屏幕亮度调高或调低一格 | 10sec |
| 把浏览器里打开的第一个标签页关掉 | 10sec |

### 家务启动类（home）
| 任务文案（中文示例） | 时长 |
|---|---|
| 把离你最近的一件脏衣服放进洗衣机 | 30sec |
| 把厨房垃圾袋拎到门口 | 1min |
| 把被子铺平，枕头摆好 | 2min |
| 把脚边的袜子捡起来放进脏衣篮 | 30sec |
| 把明天要穿的一套衣服平铺在床头 | 2min |
| 把桌上最靠近手边的一个碗拿到水槽里 | 30sec |
| 把茶几上的零食袋折起来放进抽屉 | 1min |
| 把地上离你最近的三样东西捡起来放好 | 2min |

### 沟通启动类（communication）
| 任务文案（中文示例） | 时长 |
|---|---|
| 给通讯录里第一个联系人发一个句号 | 30sec |
| 打开微信，给置顶第一个人发一个表情 | 30sec |
| 打开通讯录，翻到字母A开头的第一个人 | 1min |
| 把手机上所有应用的红角标点掉 | 1min |

### 健康启动类（health）
| 任务文案（中文示例） | 时长 |
|---|---|
| 穿上离你最近的一双鞋，站在原地踮脚三次 | 1min |
| 站起来，双手举过头顶，伸展五次 | 1min |
| 走到离你最近的窗户前，向外看十秒 | 1min |
| 倒一杯水，喝三口 | 1min |
| 把手机屏幕朝下扣在桌上 | 10sec |
| 闭上眼睛，数自己的呼吸，数到五 | 1min |

### 行政启动类（admin）
| 任务文案（中文示例） | 时长 |
|---|---|
| 在纸上写下"开始"两个字 | 30sec |
| 把桌上最上面的三张纸叠整齐 | 2min |
| 把钱包里的所有卡片拿出来，再放回去 | 1min |
| 把包里的东西全部倒出来，再放回去 | 2min |

**任务属性**：`id`, `text_key` (i18n key), `duration_seconds`, `category` (work/home/communication/health/admin), `is_preset` (bool), `created_by` (userID or null), `is_active` (bool)

---

## 心理学理论映射

| 疗法 | 核心融入 |
|---|---|
| **行为激活（BA）** | 动机随行动产生。3秒倒计时强迫在"感觉准备好"之前启动。每个任务都是行为实验，证伪"我做不到"的预测。 |
| **接纳承诺疗法（ACT）** | "跳过"按钮可用但低对比度，不批评逃避行为。执行界面只有当前任务，无未来参照。任务文案以"启动"为导向，降低心理承诺门槛。 |
| **CBT + 暴露疗法** | 倒计时强迫行为实验在冗长思考之前开始。反复强迫启动通过习惯化降低预期焦虑。UI极简阻止回避反应。 |

---

## 技术方案

- **产品名**：此刻
- **界面语言**：按浏览器语言自动适配（`vue-i18n`），核心支持 zh-CN / en-US
- **声音**：无
- **技术栈**：前后端分离
  - 后端：Go (Gin/Echo) + PostgreSQL/SQLite
  - 前端：Vue3 + Vite + Pinia
- **认证**：JWT (access token + refresh token)
- **存储**：后端数据库持久化

### 数据库核心表

```sql
-- users
id UUID PK
email VARCHAR UNIQUE
password_hash VARCHAR
created_at TIMESTAMP

-- tasks
id UUID PK
text_key VARCHAR -- i18n key
duration_seconds INT
category VARCHAR -- work | home | communication | health | admin
is_preset BOOLEAN DEFAULT false
created_by UUID NULL FK(users.id)
is_active BOOLEAN DEFAULT true
created_at TIMESTAMP

-- user_task_pool (用户可见任务关联)
user_id UUID FK
task_id UUID FK
added_at TIMESTAMP
PK(user_id, task_id)

-- task_history (轻量记录，用于排除近期重复)
id UUID PK
user_id UUID FK
task_id UUID FK
started_at TIMESTAMP
ended_at TIMESTAMP
outcome VARCHAR -- completed | skipped
created_at TIMESTAMP
```

---

## 防回避设计（延后讨论）

当前版本仅保留基础占位：
- 执行界面全屏、无菜单。
- "跳过"按钮不做长按延迟。
- 后续迭代可加入：跳过冷却期、tab切换暂停等。

---

## 项目文件结构

```
/home/sunao/.qoder/worktree/xuanting/feSCgJ
├── backend/
│   ├── main.go
│   ├── go.mod
│   ├── internal/
│   │   ├── config/
│   │   ├── models/
│   │   ├── handlers/
│   │   ├── middleware/
│   │   └── db/
│   └── ...
├── frontend/
│   ├── src/
│   │   ├── main.js
│   │   ├── router/
│   │   ├── stores/
│   │   ├── views/
│   │   ├── api/
│   │   └── locales/
│   ├── package.json
│   └── vite.config.js
└── 需求文档.md
```

---

## 验收标准

1. 启动后端与前端正向代理，前端 `npm run dev` 正常访问。
2. 未登录访问首页 → 自动跳转登录页。注册新账号 → 自动登录并跳转到派发页。
3. 派发页在1秒内显示任务卡片，3秒倒计时后自动进入 Mission HUD。
4. Mission HUD 显示倒计时进度环和"完成了"/"跳过"按钮。
5. 点击"完成了" → Victory 页面 → 点击"继续" → 获取新任务。
6. 进入自定义任务页，添加新任务 → 保存后该任务可被后续派发选中。
7. 切换浏览器语言为 en-US，刷新页面，界面及任务文本自动变为英文。
8. 关闭并重新打开浏览器，若 token 未过期，保持登录状态并直接进入派发页。
